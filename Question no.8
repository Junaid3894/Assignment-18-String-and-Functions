/*8. Write a function to count words in a given string*/

#include<stdio.h>
int count_words(char *str,int n);
int main()
{
char str[100];
int length=-1;
printf("Enter the string :");
fgets(str,100,stdin);
for(int i=0;str[i];i++){
   length++;
}
printf("Your string have %d words ",count_words(str,length));


return 0;
}
int count_words(char *str,int n){
   int count=1;
   int i;
    for(i=0;i<n;i++){

      if(str[i]==32){
         count++;

      }
    }
    return count;
}
